# ignite-cli

This module contains the build-time CLI generation engine for IgniteBoot.

Current status: design-only skeleton. Source roots created but no implementation yet.

Responsibilities:
- Parse project templates and YAML descriptors
- Produce generated Java sources or archetype artifacts

For now this module only provides the module pom and source root placeholders.
