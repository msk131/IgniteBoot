# Quickstart (Conceptual)

1. `ignite-cli gen api.yaml` — generate DTOs, handlers, routing.
2. Implement handler logic in the handwritten folder.
3. `ignite-cli run` — bootstrap the explicit runtime and serve requests.

(These are conceptual commands; see CLI docs for options and CI integration.)
