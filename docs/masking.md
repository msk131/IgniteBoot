# Masking (High Level)

Placement: outbound serialization interceptor (apply masking at write time).

Rules: in-memory only; role-based resolution; deterministic placeholders; reversible unmask requires HSM-backed signed approval and audit logging.
