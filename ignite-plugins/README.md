# ignite-plugins

This module holds code-generation target blueprints and templates used by the CLI engine.

Current status: design-only skeleton. Source roots created but no templates yet.

Responsibilities:
- Provide template projects and plugin descriptors consumed by ignite-cli

For now this module only provides the module pom and source root placeholders.
